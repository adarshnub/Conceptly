# Chapter 1 prerecorded narration

Run `npm run audio:generate:chapter1` with a valid OpenAI API key to generate one MP3 per teaching beat in this directory.

The application serves these AI-generated recordings directly and never calls a class-session speech API at learner runtime. Missing files automatically fall back to the learner's browser voice.
